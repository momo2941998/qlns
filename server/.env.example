# Environment
NODE_ENV=development

# Server Port
PORT=8089

# MongoDB Connection
MONGO_URI=mongodb://admin:password123@localhost:27024/qlns?authSource=admin

# Upload Directory
# Đường dẫn thư mục lưu file upload (relative hoặc absolute)
# Ví dụ:
#   - Relative: uploads/avatars (sẽ tạo trong thư mục project)
#   - Absolute: /var/www/uploads/avatars
UPLOAD_DIR=uploads/avatars
